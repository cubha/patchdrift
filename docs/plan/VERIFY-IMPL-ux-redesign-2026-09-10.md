# verify-impl — UX 개편 인수검증 (2026-09-10)

기준선
- 확정 시안 아티팩트 `https://claude.ai/code/artifact/2eb4f276-891a-4634-99cd-31834671f728` (HANDOFF §9 지정 원본)
- `docs/design/HANDOFF-redesign-2026-09-10.md`
- `docs/plan/PLAN-ux-redesign-2026-09-10.md`

대상: ST-A~M 13개 SubTask (커밋 `8bfa788..73976c7`)
방법: `out/` 정적 빌드를 127.0.0.1:4173로 서빙, 1280×800, 시안 DOM 트리 추출 후 구조 질의 대조. 픽셀 대조 없음.

## 축A — 요구사항·계획 대비 코드
직전 Phase 3 배치검증(scope-critic 13 + acceptance-critic 1) 결과 승계. 재판정하지 않음(재검증 규약 §1).
- UNMET 1 — 히어로 스플래시 아트(PLAN 승인 절단)
- UNREQUESTED 0

## 축B — 시안 대비 화면

### 팔레트 (4화면 공통) ✅
V4 토큰 15종 전량 시안과 동일(`mismatchVsV4: []`). `--focus-ring: 0 0 0 4px #c8a35547`, body bg `rgb(3,13,24)`.

### `/` ↔ 시안 04 홈
- ✅ 요소 유무 — 헤더·필터칩·히어로·노트×관측 스트림·라인별 미공지 분포·매치 평균·디스코드 패널 전부 존재
- ✅ 스킬 행 — 스펠 아이콘 실렌더(AurelionSol 56px + Q/W 40px), `A ⇒ B` 표기 시안과 동일
- ❌ 스트림 순서 — 미공지 151그룹이 전부 상단, 첫 짝지음 그룹 index 151. 시안은 "노트 순서 그대로 + 미공지를 **그 자리에** 삽입"
- ❌ `.rn-obs`(엔티티 레벨 관측 요약 1줄) 부재, `.verdict .m`(판정 근거 1줄: "노트=상향 · 관측=밴률 폭증") 부재. `.gap-why`가 엔티티 1회가 아니라 metric 행마다 반복
- ⚠️ 라인 필터 위치 — 시안 `.m-filter`(상단 필터바) vs 구현 본문 컬럼 내부
- ⚠️ 라인 어휘 — 필터 라벨 "원딜/서포터"(format.ts SSOT) vs 시안 "바텀/서폿". 구현 내부도 혼재(글리프 title="원딜", 엔티티명="바텀")
- ⚠️ 접근성 — LaneGlyph `<title>`이 버튼 내부라 접근명이 "전체전체"·"탑탑"으로 중복(`aria-label` 부재)
- ⏭ 시안 `.thin-box`(얇은 표본 안내) — HANDOFF 142~143행의 운영 주석 성격이라 제품 요소로 보지 않음. 미대응 정상

### `/compare` ↔ 시안 05 대조표
- ✅ 노트 사이드바 아이콘 40px 실렌더, 탭 카운트 "챔피언 104 / 아이템 10 / 시스템 51" 시안과 일치
- ✅ 라인 행 글리프 박스 — 시안이 지적한 `골` 텍스트 박스 대체됨. 라인 태그 "탑 · 승률" 표기 일치
- ✅ 커버리지 바 — "노트 35엔티티(215항목) 중 관측 짝 242 · 미공지 277 · 표본 부족 585"
- ❌ 라인 필터 부재 — 시안 `.m-filter > .lanes`가 대조표에도 있음
- ⚠️ 엔티티 셀 라벨 중복 — "원딜바텀"(글리프 title + 엔티티명)
- ⚠️ 컬럼 10개 vs 시안 7개(엔티티·전·후·Δ·95%CI·n·상태)

### `/item/[id]` ↔ 시안 06 항목 상세
- ✅ **오차막대 3개 실렌더** — 시안의 최대 지적("범례는 95% CI를 약속하고 오차막대를 안 그린다") 해소
- ✅ 차트 폭 축소(`max-w-xl`), 아이콘 72px, 통계 게이트 4값(n 10,000/10,000 · ±1.3%p · q 0.000) 시안과 동일
- ⚠️ 범례에 CI 실측값 `[46.3, 48.2] · [33.3, 35.2]` 미표기(시안은 mono로 병기)
- ⚠️ 패널 배치 — 시안 1fr/320px(좌=차트, 우=[게이트, 대조]) vs 구현 좌=[대조, 관측값, 추정원인] / 우=[게이트, 원천 매치]

### `/methodology` ↔ 시안 07 방법론
- ✅ 8행 매핑표 존재, 레이아웃 유지
- ❌ 4번째 컬럼이 "PUBG 상태" — 시안은 **"어댑터 인터페이스"**(`NoteSource.fetch()` / `MatchSource.collect()` / `Entity{type,key,name}` / `Segment[]` / `Metric.adoption` / `Metric.outcome` / `Metric.timeline` / `AssetSource.icon()` / 고정)
- ❌ 판정 엔진이 표 마지막 행이 아니라 표 아래 문단. 시안 명시: "마지막 행이 핵심입니다 — 바뀌는 것은 어댑터 8줄이고 판정 엔진은 고정"

### 결정론 보조 (design-lint)
- 시안 아티팩트: error 1 · warn 11 (FAIL)
- 구현 `out/index.html`: error 0 · warn 0 (PASS)
→ 토큰 위생은 구현이 시안보다 깨끗. 시안 대비 하드코딩 이탈 없음.

### 런타임
- 콘솔 error 1건: `GET /dd/item/4402.png 404` — 아이템 아이콘 자산 누락(이번 개편 범위 밖, DDragon 동기화 누락)
- 경고 2건은 Next 폰트 preload 관련(무해)

## 결론
미충족 항목 있음 — 팔레트·자산·오차막대 등 시안의 핵심 지적은 해소됐고, 남은 이탈은 정보 위계·어휘·표 구성 층위.

UNMET: 11 (축A 1 + 축B 10) · UNREQUESTED: 0
