// scripts/run-match.ts
// F3~F4 파이프라인 진입점 — dotenv 로드 후 패치노트 파싱 + 짝짓기 + 판정 호출.
// 실행: npm run pipeline:match
// TODO(F3/F4): parsePatchNotes → matchDeterministic → inferIndirectCandidates → buildDeltaRecords 연결

import "dotenv/config";

async function main(): Promise<void> {
  console.log("TODO");
}

main();
